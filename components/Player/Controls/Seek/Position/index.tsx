const PlayBackPosition = ({ value }: any) => (
  <div
    data-testid="playback-position"
    className="Type__TypeElement-sc-goli3j-0 fcYQUS playback-bar__progress-time-elapsed"
    data-encore-id="type"
  >
    {value}
  </div>
);

export default PlayBackPosition;
